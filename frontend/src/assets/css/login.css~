/* 기본 스타일 */
body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f8f9fa;
    text-align: center;
}

/* 네비게이션 바 */
.top-nav {
    background: white;
    padding: 15px 0;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
}

.container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: auto;
    padding: 0 20px;
}

.logo {
    font-size: 24px;
    font-weight: bold;
    color: #007bff;
    text-decoration: none;
}

.menu a {
    margin: 0 10px;
    text-decoration: none;
    color: #333;
}

.menu .btn {
    padding: 8px 15px;
    background: #007bff;
    color: white;
    border-radius: 5px;
    text-decoration: none;
}

.menu .btn.active {
    background: #0056b3;
}

/* 로그인 폼 */
.login-container {
    display: flex;
    justify-content: center;
    align-items: flex-start;   /* 위쪽 정렬 */
    height: 70vh;              /* 전체 높이 축소 */
    padding-top: 40px;         /* 위쪽 여백 */
}

.login-box {
    display: flex;
    flex-direction: column;
    align-items: center;
    background: white;
    padding: 40px 32px;
    border-radius: 14px;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.12);
    width: 380px;
    min-width: 320px;
}

.login-box h2 {
    margin-bottom: 18px;
    font-size: 2rem;
}

.login-box p {
    font-size: 14px;
    color: #666;
}

form {
    display: flex;
    flex-direction: column;
    text-align: left;
    width: 100%;
}

label {
    font-size: 14px;
    margin: 10px 0 5px;
}

input {
    padding: 14px;
    font-size: 1rem;
    border: 1px solid #ccc;
    border-radius: 7px;
    margin-bottom: 8px;      /* 입력창 아래 간격 축소 */
    width: 100%;
    box-sizing: border-box;
}

.login-btn {
    margin-top: 8px;         /* 버튼 위 간격 축소 */
    background: #007bff;
    color: white;
    padding: 14px;
    font-size: 1rem;
    border: none;
    border-radius: 7px;
    cursor: pointer;
    width: 100%;
    transition: background 0.2s;
}

.login-btn:hover {
    background: #0056b3;
}

.login-links {
    margin-top: 20px;        /* 로그인 버튼과 링크 사이 간격 */
    text-align: center;
    width: 100%;
}

.login-links a {
    font-size: 12px;
    text-decoration: none;
    color: #007bff;
    margin: 0 10px;          /* 링크 간격 추가 */
}

